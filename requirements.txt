# ── Computer vision & pose estimation ───────────────────────────────
# mediapipe 0.10.14 ships BOTH mp.solutions.pose (legacy, reliable) AND
# the Tasks API. Versions >= 0.10.18 removed mp.solutions entirely.
mediapipe==0.10.14
opencv-python>=4.8.0
numpy>=1.24.0

# ── Web API server ───────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
